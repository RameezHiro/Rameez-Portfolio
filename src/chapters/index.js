// Compatibility re-export of the canonical chapter registry.
// Import from here or from '../data/chapters' — both resolve to the same data.
export { chapters, getChapterById, getChapterBySlug, default } from '../data/chapters.js';
